# Epsilon AI Inference Service Requirements for RunPod
# Created by Neural Operations & Holdings LLC
# EXACT VERSIONS - NO FALLBACKS

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
click>=8.1.0,<9.0.0  # Required by uvicorn, ensure compatibility

# ML/AI libraries - CRITICAL: PyTorch 2.8.0+cu128 requires triton==3.4.0 EXACTLY
# Install PyTorch from PyTorch index (comes with CUDA)
# torch==2.8.0+cu128  # Install separately: pip install torch --index-url https://download.pytorch.org/whl/cu128
triton==3.4.0  # MUST match PyTorch version - PyTorch 2.8.0 requires triton==3.4.0
# CRITICAL: Use latest transformers from GitHub to support gpt_oss architecture
# transformers==4.46.0  # Old version doesn't support gpt_oss - causes KeyError
# Install from GitHub: pip install -U git+https://github.com/huggingface/transformers.git
# (Installed separately in clean_install.sh to ensure correct version)
accelerate==0.34.0
bitsandbytes==0.44.0

# Utilities
python-dotenv==1.0.1
psutil==6.1.0
pydantic==2.9.2
protobuf==5.28.3
filelock==3.16.1
huggingface_hub==0.26.0
tqdm>=4.66.0  # Required for download progress bars
# hf-transfer is optional (only speeds up downloads) - install separately if needed
# hf-transfer>=0.1.9

