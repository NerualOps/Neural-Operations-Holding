# Epsilon AI Python Services Dependencies
# © 2025 Neural Operation's & Holding's LLC. All rights reserved.

# Core web framework (minimal dependencies)
fastapi>=0.100.0
uvicorn>=0.20.0

# Basic utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Database connectivity
asyncpg>=0.28.0

# HTTP client for Supabase REST API
aiohttp>=3.8.0

# PDF processing
PyPDF2>=3.0.0
pdfplumber>=0.9.0
pdf2image>=1.16.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Machine learning / Transformer model (for inference service)
# NOTE: torch is installed separately in build command (CPU-only for production)
# torch>=2.0.0  # Installed separately as CPU-only version
numpy>=1.24.0,<2.0.0
tokenizers>=0.13.0
supabase>=2.0.0  # For model bootstrap from Supabase Storage